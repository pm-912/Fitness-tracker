const { User, Workout } = require('../models')

const resolvers = {
    Query: {
        allWorkouts: async () => {
            
        }
    },

    Mutation: {

    }
};

module.exports = resolvers;